import React from "react";
import styles from "./Header.module.scss";

const HeaderDefaultBackground = () => (
  <div className={styles["header__default-bg"]}></div>
);

export default HeaderDefaultBackground;
